name: opsmanual_ai
publish_to: 'none'
description: OpsManual AI - multi-tenant operations manual platform
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  firebase_core: ^3.2.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.1.0
  cloud_functions: ^5.0.0
  firebase_storage: ^12.1.0
  image_picker: ^1.1.2
  uuid: ^4.4.0

flutter:
  uses-material-design: true
